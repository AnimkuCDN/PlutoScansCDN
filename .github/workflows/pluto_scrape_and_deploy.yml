name: Auto Scrape PlutoScans Manga & Deploy CDN

on:
  schedule:
    - cron: '0 2 * * *' # Menjalankan otomatis setiap hari jam 02:00 UTC
  workflow_dispatch:
    inputs:
      pages:
        description: 'Jumlah halaman per genre (0 = Semua Halaman)'
        required: false
        default: '0'

permissions:
  contents: write

jobs:
  scrape-and-commit:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          cache: 'pip'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run PlutoScans Scraper on GitHub Cloud Runner
        run: |
          PAGES="${{ github.event.inputs.pages || '0' }}"
          python pluto_scraper.py \
            --max-pages $PAGES \
            --github-user "${{ github.repository_owner }}" \
            --github-repo "${{ github.event.repository.name }}" \
            --branch "${{ github.ref_name || 'main' }}" \
            --threads 16

      - name: Commit and Push PlutoScans CDN updates
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add cdn/
          if git diff --staged --quiet; then
            echo "Tidak ada pembaruan komik PlutoScans baru."
          else
            git commit -m "chore(cdn): auto scrape plutoscans manga & update catalog [skip ci]"
            git push
          fi
